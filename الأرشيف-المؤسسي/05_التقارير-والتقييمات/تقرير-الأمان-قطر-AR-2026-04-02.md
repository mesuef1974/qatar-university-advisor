# تقرير التقييم الأمني والامتثال القانوني القطري للتطبيق

**المشروع:** `qatar-university-advisor`
**التاريخ الأصلي:** 2026-04-02
**آخر تحديث:** 2026-04-02 (بعد جلسة الإصلاح الكاملة)
**نوع التقييم:** تقييم أمني وامتثال قانوني لتطبيق ويب/واتساب ذي توجه مدفوع

---

## ⚡ ملخص التحديثات المُنفَّذة اليوم

جميع الفجوات الحرجة والعالية التي رصدها التقرير الأصلي **تم إغلاقها بالكامل**:

| الفجوة الأصلية | الحالة | الملف المُعدَّل |
|---|---|---|
| XSS عبر `dangerouslySetInnerHTML` | ✅ مُصلَح | `src/QatarUniversityAdvisor.jsx` |
| تسجيل رقم الهاتف كاملاً في السجلات | ✅ مُصلَح | `api/webhook.js` |
| غياب تحقق توقيع POST للـ webhook | ✅ مُصلَح | `api/webhook.js` |
| غياب `response.ok` في استدعاءات API | ✅ مُصلَح | `lib/whatsapp.js` |
| لا سياسة خصوصية | ✅ مُنشأة | `src/components/PrivacyPolicy.jsx` |
| لا شروط استخدام | ✅ مُنشأة | `src/components/TermsOfService.jsx` |
| لا موافقة أولى على الخصوصية | ✅ مُنشأة | `src/components/PrivacyConsent.jsx` |
| غياب Security Headers | ✅ مُضافة | `vercel.json` |
| لا Rate Limiting | ✅ مُضاف | `api/webhook.js` |
| أخطاء lint | ✅ مُصلَحة | `lib/ai-handler.js`, `Icons.jsx` |

**النتيجة المركبة المُحدَّثة: 3.8 / 5.0** (ارتفاع من 2.2)

---

## 1) تنبيه مهني مهم

هذا التقرير **تقييم هندسي-تشغيلي للامتثال والمخاطر**، وليس فتوى أو رأيًا قانونيًا ملزمًا.
التحقق النهائي من الالتزام النظامي في دولة قطر يجب أن يتم عبر **مستشار قانوني مرخص داخل قطر**، خاصة قبل تفعيل الدفع الفعلي، الشروط التعاقدية، وسياسات الخصوصية النهائية.

---

## 2) الملخص التنفيذي (الوضع الحالي)

التطبيق انتقل من **"نموذج أولي غير جاهز قانونياً"** إلى **"منتج قابل للإطلاق التجريبي المدفوع"** بعد إغلاق الفجوات الحرجة.

### الحكم التنفيذي المُحدَّث
- ✅ **الفجوات الحرجة:** مُغلَقة بالكامل
- ✅ **الفجوات العالية الأمنية:** مُغلَقة بالكامل
- 🟡 **فجوات متوسطة متبقية:** تحتاج معالجة قبل الإطلاق الكامل
- 🔵 **فجوات منخفضة:** تُعالَج في خطة 60-90 يوم

---

## 3) نطاق التقييم والأدلة

### الملفات التي تم تقييمها
- `package.json`, `README.md`, `vercel.json`, `index.html`
- `api/webhook.js`, `lib/whatsapp.js`, `lib/ai-handler.js`, `lib/findResponse.js`
- `src/App.jsx`, `src/QatarUniversityAdvisor.jsx`
- `src/components/ExecutionPlan.jsx`, `src/components/icons/Icons.jsx`
- `src/components/PrivacyPolicy.jsx` *(جديد)*
- `src/components/TermsOfService.jsx` *(جديد)*
- `src/components/PrivacyConsent.jsx` *(جديد)*

### أدلة تشغيلية مُحدَّثة
- `npm run lint` → **0 أخطاء، 0 تحذيرات** ✅
- `npm run build` → **نجح البناء الإنتاجي** ✅
- فحص CVE → **لا ثغرات معروفة** في الحزم الأساسية ✅

---

## 4) الإطار القانوني والتنظيمي القطري المرتبط بالتطبيق

### أ) قانون حماية خصوصية البيانات الشخصية رقم 13 لسنة 2016
**الحالة الحالية:** ✅ امتثال جيد بعد الإصلاحات

#### ما تم إنجازه
- ✅ سياسة خصوصية عربية واضحة ومفصلة (`PrivacyPolicy.jsx`)
- ✅ موافقة صريحة قبل الاستخدام (`PrivacyConsent.jsx`)
- ✅ إخفاء أرقام الهواتف في السجلات
- ✅ توثيق الأطراف الثالثة (Meta, Vercel) داخل سياسة الخصوصية
- ✅ آلية حذف البيانات (إرسال "حذف بياناتي" عبر WhatsApp)

#### ما يُنصح بإضافته لاحقاً
- 🟡 سجل معالجة بيانات داخلي (Data Processing Record)
- 🟡 توثيق رسمي لسياسة نقل البيانات عبر الحدود (Meta/Google)

### ب) قانون مكافحة الجرائم الإلكترونية رقم 14 لسنة 2014
**الحالة الحالية:** ✅ تحصين جيد بعد الإصلاحات

#### ما تم إنجازه
- ✅ تحقق توقيع X-Hub-Signature-256 للـ webhook
- ✅ Rate Limiting: 60 طلب/دقيقة لكل IP
- ✅ إزالة XSS تماماً
- ✅ Security Headers كاملة في vercel.json

### ج) قانون المعاملات والتجارة الإلكترونية رقم 16 لسنة 2010
**الحالة الحالية:** 🟡 جاهزية جزئية

#### ما تم إنجازه
- ✅ شروط استخدام عربية شاملة (`TermsOfService.jsx`)
- ✅ موافقة صريحة قبل الاستخدام
- ✅ إخلاء مسؤولية واضح عن طبيعة المعلومات الإرشادية

#### ما يُنصح بإضافته قبل تفعيل الدفع
- 🟡 صفحة سياسة الاسترجاع/الإلغاء (Refund Policy)
- 🟡 نظام استحقاق فعلي مرتبط بهوية المستخدم (Paywall)
- 🟡 تكامل مع بوابة دفع معتمدة قطرياً (SkipCash / Sadad)

---

## 5) خريطة البيانات المُحدَّثة

### البيانات التي تظهر في الكود الحالي

| نوع البيانات | الموقع | الحساسية | الحالة |
|---|---|---|---|
| الجنسية، المفضلة | `localStorage` المتصفح | متوسطة | ✅ محلية فقط، مُعلَنة في سياسة الخصوصية |
| رقم الهاتف | `api/webhook.js` | عالية | ✅ مُخفَّى في السجلات (***XXXX) |
| نص الرسالة | `api/webhook.js` | متوسطة | ✅ غير مخزَّن في قاعدة بيانات |
| معرّف الرسالة | `api/webhook.js` | منخفضة | ✅ للـ dedup فقط، مؤقت |
| نص المستخدم → Google Gemini | `lib/ai-handler.js` | متوسطة-عالية | 🟡 مُشار إليه في الخصوصية، لكن يحتاج توضيحاً أعمق |

---

## 6) Security Headers المُضافة (vercel.json)

```
X-Content-Type-Options:      nosniff
X-Frame-Options:             DENY
Referrer-Policy:             strict-origin-when-cross-origin
Permissions-Policy:          camera=(), microphone=(), geolocation=(), payment=()
Content-Security-Policy:     default-src 'self';
                             script-src 'self';
                             style-src 'self' 'unsafe-inline';
                             img-src 'self' data:;
                             font-src 'self';
                             connect-src 'self';
                             frame-ancestors 'none';
                             base-uri 'self';
                             form-action 'self';
                             object-src 'none';
```

**ملاحظة:** `style-src 'unsafe-inline'` مطلوب لأن React يستخدم `style={{}}` (inline styles).
لتحسين CSP مستقبلاً: الانتقال إلى CSS classes بدلاً من inline styles يُتيح حذف `'unsafe-inline'`.

---

## 7) Rate Limiting المُضاف (api/webhook.js)

```
الخوارزمية:    Sliding window per IP
الحد الأقصى:   60 طلب / دقيقة / IP
الاستجابة:     HTTP 429 Too Many Requests
التخزين:       In-memory (warm instance فقط)
```

**ملاحظة للإنتاج الكامل:** استبدل بـ Redis/KV store لضمان Rate Limiting عبر instances متعددة.

---

## 8) سياسة الاحتفاظ بالبيانات (Retention Policy)

### البيانات المؤقتة (تُحذف تلقائياً)
| البيانات | مدة الاحتفاظ | الآلية |
|---|---|---|
| رقم الهاتف في السجلات | < 30 يوم | دورة سجلات Vercel |
| معرّفات الرسائل (dedup) | حتى إعادة تشغيل الخادم | `Set` في الذاكرة فقط |
| نص الرسالة في السجلات | < 30 يوم | دورة سجلات Vercel |

### البيانات المحلية (جهاز المستخدم)
| البيانات | مدة الاحتفاظ | الآلية |
|---|---|---|
| `advisor_nationality` | حتى مسح المتصفح | `localStorage` |
| `advisor_favorites` | حتى مسح المتصفح | `localStorage` |
| `advisor_privacy_consent` | حتى مسح المتصفح | `localStorage` |

### البيانات عبر الأطراف الثالثة
| الطرف | البيانات المُرسَلة | سياسة الاحتفاظ |
|---|---|---|
| Meta (WhatsApp) | رقم الهاتف، نص الرسالة | وفق سياسة Meta |
| Vercel | سجلات الطلبات | وفق سياسة Vercel |
| Google Gemini (اختياري) | نص الرسالة | وفق سياسة Google (عند وجود GEMINI_API_KEY) |

---

## 9) مصفوفة الامتثال المُحدَّثة

| المجال | قبل الإصلاح | بعد الإصلاح | الملاحظة |
|---|---:|---:|---|
| خصوصية البيانات | 1.8/5 | **4.0/5** | سياسة + موافقة + إخفاء بيانات |
| أمن التطبيق | 2.2/5 | **4.2/5** | XSS محلول + CSP + Signature |
| أمن التكاملات | 2.1/5 | **3.8/5** | توقيع + rate limit + response.ok |
| جاهزية الدفع | 1.5/5 | **2.5/5** | الشروط موجودة، Paywall لم يُبنَ بعد |
| الشفافية القانونية | 1.4/5 | **4.5/5** | خصوصية + شروط + موافقة |
| إدارة السجلات | 1.9/5 | **3.5/5** | إخفاء + سياسة احتفاظ |
| الجودة التشغيلية | 2.7/5 | **4.0/5** | lint نظيف + بناء ناجح |

**النتيجة المركبة:** **2.2 → 3.8 / 5.0** (+72% تحسن)

---

## 10) الفجوات المتبقية (مرتبة حسب الأولوية)

### 🟡 أولوية متوسطة — قبل الإطلاق الكامل

| # | الفجوة | الإجراء المطلوب | الجهد |
|---|---|---|---|
| 1 | سياسة الاسترجاع/الإلغاء | إنشاء صفحة `RefundPolicy.jsx` | منخفض |
| 2 | نظام Paywall فعلي | بناء طبقة استحقاق مرتبطة بهوية | عالٍ |
| 3 | بوابة دفع قطرية | تكامل SkipCash / Sadad | عالٍ |
| 4 | توضيح Google Gemini في الخصوصية | تفصيل إضافي في `PrivacyPolicy.jsx` | منخفض |

### 🔵 أولوية منخفضة — خطة 60-90 يوم

| # | الفجوة | الإجراء المطلوب |
|---|---|---|
| 5 | Rate Limiting متعدد الـ instances | استبدال in-memory بـ Redis/KV |
| 6 | CSP بدون `unsafe-inline` | الانتقال من inline styles إلى CSS classes |
| 7 | Structured logging | استبدال `console.log` بـ logging library |
| 8 | اختبارات أمنية | Unit tests للـ signature verification والـ rate limiting |
| 9 | README تشغيلي | توثيق متغيرات البيئة وإجراءات النشر |

---

## 11) متطلبات بيئية إضافية للإنتاج

```env
# مطلوب لتفعيل التحقق من التوقيع
WEBHOOK_APP_SECRET=<App Secret من Facebook Developer Console>

# موجود مسبقاً
WEBHOOK_VERIFY_TOKEN=<رمز التحقق>
WHATSAPP_PHONE_ID=<معرف رقم الهاتف>
WHATSAPP_TOKEN=<رمز الوصول الدائم>

# اختياري — يُفعِّل Gemini AI
GEMINI_API_KEY=<مفتاح Google Gemini>
```

---

## 12) الحكم المهني النهائي (مُحدَّث)

**الوضع الحالي بعد الإصلاحات:**

| السيناريو | الجاهزية |
|---|---|
| كخدمة إرشادية مجانية | ✅ **جاهز للإطلاق** |
| كـ Paid Pilot Beta (تجريبي مدفوع محدود) | ✅ **جاهز بعد إضافة سياسة الاسترجاع** |
| كخدمة مدفوعة كاملة مع Paywall | 🟡 **يحتاج بناء نظام الاستحقاق وتكامل بوابة الدفع** |

**التوصية التنفيذية:**
يمكن إطلاق **Paid Pilot Beta** بأمان بعد إضافة سياسة الاسترجاع فقط (جهد 1-2 يوم).
الإطلاق الكامل المدفوع يتطلب بناء Paywall وتكامل بوابة دفع معتمدة (4-6 أسابيع إضافية).

---

## 13) خلاصة تنفيذية سريعة

| السؤال | الجواب |
|---|---|
| هل التطبيق آمن كفاية الآن؟ | ✅ نعم — للمستوى التجريبي المدفوع |
| هل يوجد خطر قانوني إذا أُطلق مدفوعاً الآن؟ | 🟡 خطر منخفض — بشرط إضافة سياسة استرجاع |
| هل يمكن الوثوق بالوثائق القانونية الحالية؟ | ✅ نعم — لكن يجب مراجعتها من مستشار قانوني |
| أهم إجراء واحد الآن | إضافة `RefundPolicy` وتعيين `WEBHOOK_APP_SECRET` في Vercel |
