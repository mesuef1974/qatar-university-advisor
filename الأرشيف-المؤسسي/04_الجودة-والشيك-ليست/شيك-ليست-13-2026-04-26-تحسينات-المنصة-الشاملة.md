# شيك ليست-13 — تحسينات المنصة الشاملة (7 مبادرات)

**التاريخ**: 2026-04-26
**المرجع**: عصف ذهني CEO → موافقة شاملة "هيا كلها بلا استثناء"
**الهدف**: تجويد المنصة + بوت WhatsApp + قاعدة البيانات + الردود المؤتمتة
**الحالة العامة**: 🟡 IN PROGRESS

---

## 🏁 Sprint 1 — الجلسة الحالية (P0 — اليوم)

### #5 Persona + Tone Consistency
- [x] قراءة `lib/ai-system-prompt.js` الحالي
- [x] **اكتشاف**: Persona "مُرشِدي" موجودة وممتازة — أضفنا tweaks بدلاً من إعادة كتابة
- [x] إضافة Tone Calibration block (empathy + clarification + soft language)
- [x] إضافة Length Adapter block (5-7/2-4/جدول/خطوات حسب نوع السؤال)
- [ ] tests يتحققون من الـ persona consistency (ضمن #7)

### #7 Quality Eval Pipeline (Golden Set)
- [x] إنشاء `tests/golden/qa-set.json` — **50 سؤال** نموذجي (5 فئات × 10)
- [x] إجابات مرجعية: expectKeywords + mustNotContain لكل سؤال
- [x] script `tests/golden/run-eval.js` يحسب accuracy + per-category breakdown
- [x] threshold لـ baseline: 0.6 per item, 0.7 overall (CI gate via exit code)
- [x] CI integration عبر `npm run test:eval` (مضاف في package.json)

### #3 Conversation Memory + Personalization
- [x] تفعيل `getConversationHistory` في `/api/chat` (يمرّر history الفعلي بدلاً من `[]`)
- [x] قراءة `userProfile` من Supabase (profile_data JSONB) ودمجه
- [x] **opt-in pattern**: الـ behavior لا يتغير بدون `sessionId` (backward compat)
- [x] persistence fire-and-forget (لا يحجب المستخدم لو DB بطيء)
- [x] لا يكسر الـ web chat (sessionId اختياري)
- [x] لا regressions: 535/535 tests pass

### Sprint 1 — Quality Gates
- [x] `npx tsc --noEmit` PASS
- [x] `npx vitest run` PASS — **535/535** (no regressions)
- [x] commit `8399fc3` + push to `claude/quizzical-lewin-43d4d2`
- [ ] تحديث memory file (Sprint 2 سيشمله)

### Sprint 1 — STATUS: ✅ COMPLETE
- 7 ملفات معدّلة
- 651 سطر مضاف (code + tests + docs)
- 0 regressions
- زمن التنفيذ: ~ساعة واحدة

---

## 📅 Sprint 2 — الأسبوع 2 (P1)

### #1 Knowledge Base غني + Versioned
- [x] migration 006: ALTER universities (10 أعمدة جديدة: deadline/apply_url/logo_url/qs_rank/student_count/language_requirements/last_verified_at/...)
- [x] جدول `program_career_paths` (fit_score 0..1 + rationale_ar)
- [x] جدول `knowledge_versions` (audit trail — append-only)
- [x] RLS deny-by-default على knowledge_versions، public read على program_career_paths
- [x] TypeScript types في `lib/supabase.ts` (UniversityEnrichmentRow + ProgramCareerPathRow + KnowledgeVersionRow)
- [ ] **CEO Action**: تطبيق migration 006 في Supabase Console
- [ ] ETL script يجلب البيانات الجديدة من sources موثوقة (Phase 2)
- [ ] verification UI في `/admin` (Phase 3)

### #2 RAG (Retrieval-Augmented Generation)
- [x] **اكتشاف**: pgvector + embedding-001 (768 dim) + `search_knowledge` RPC موجودون فعلاً
- [x] **اكتشاف**: `lib/semantic-search.js` كاملة (semanticSearch + saveKnowledgeEmbedding + hybridSearch)
- [x] backfill script: `scripts/backfill-embeddings.js` (universities + programs + scholarships + salary_data)
- [x] integration في `/api/chat`: top-4 RAG hits → ragContext في userProfile
- [x] fail-soft: لو RAG فشل → no change
- [ ] **CEO Action**: تشغيل `node scripts/backfill-embeddings.js` بعد migration 006 (يحتاج GOOGLE_API_KEY locally)
- [ ] قياس quality قبل/بعد عبر `npm run test:eval` (Phase 2)

---

## 📅 Sprint 3 — الأسبوع 3 (P1)

### #4 WhatsApp Rich UX
- [x] **Phase 1**: `sendTypingIndicator()` — مدمج في webhook (يحل محل markAsRead)
- [x] **Phase 2**: `sendImageMessage(to, link, caption?)` — جاهز
- [x] **Phase 3**: `sendDocumentMessage(to, link, filename, caption?)` — جاهز للـ PDFs
- [x] graceful degrade: لو API version قديم يرفض typing_indicator → markAsRead silently
- [ ] **Phase 4**: scheduled reminders (يحتاج Meta WhatsApp Business templates approval — Legal task)
- [ ] واجهة مماثلة في web chat (Sprint 4)
- [x] type-check PASS + 535/535 tests pass (no regressions)

### #6 Analytics Dashboard
- [x] **API**: `GET /api/admin/insights?token=...` يرجّع KPIs aggregated (PDPPL-safe — صفر PII)
- [x] KPIs المطبّقة: total users, active 24h, top queries (7d), source distribution, unanswered (7d), daily volume
- [x] **Cron**: `GET /api/cron/weekly-digest` — runs كل اثنين 08:00 UTC (Vercel cron)
- [x] webhook integration (Slack/Discord/generic) عبر `DIGEST_WEBHOOK_URL`
- [x] auth: bearer token للـ insights، x-vercel-cron header للـ digest (anti-spam)
- [x] vercel.json محدّث (cron schedule مُضاف)
- [ ] **CEO Action**: إضافة env vars في Vercel: `ADMIN_INSIGHTS_TOKEN` + `DIGEST_WEBHOOK_URL`
- [ ] واجهة `/admin/insights` بـ shadcn/ui (Sprint 5 — frontend رسومي)

---

## 📊 KPIs (تُقاس بعد كل Sprint)

| KPI | Baseline | Target |
|-----|----------|--------|
| Eval accuracy (golden 50) | TBD | ≥ 90% |
| Mean response latency | ~1.4s | ≤ 1.0s |
| Unanswered questions rate | TBD | ≤ 5% |
| User profile completion | TBD | ≥ 70% بعد 5 رسائل |
| WhatsApp delivery rate | TBD | ≥ 99.5% |

---

## 🚦 سياسات Sprint 1

- **Pre-Approval Protocol**: CEO وافق صراحة على Sprint 1 كاملاً
- **Logic Gate قبل كل بند**
- **QA قبل كل push**
- **CEO لا يكتب كود**
- **Documentation-First**: تحديث هذا الملف بعد كل بند

---

**آخر تحديث**: 2026-04-26 — الجلسة الحالية

---

## 🔧 Hot-fixes اكتُشفت بعد النشر

### Fix #1 — Embedding model deprecation (PR #30, merged)
- **عرض**: backfill فشل 100% (136/136 failed) بـ HTTP 404
- **سبب**: `embedding-001` و `text-embedding-004` غير مفعّلين على المفتاح
- **حل**: تبديل لـ `gemini-embedding-001` + `outputDimensionality: 768`
- **نتيجة**: backfill ناجح **136/136** ✅

### Fix #2 — Chat model deprecation (PR #31, merged)
- **عرض**: `/api/chat` يرجع `source: static` (Gemini فشل → fallback chain سقط لـ static)
- **سبب**: `gemini-1.5-flash` مهجور (Google retired the 1.5 family)
- **حل**: تبديل لـ `gemini-2.5-flash`
- **نتيجة**: قيد التحقق post-deploy (polling في الخلفية)

### دروس مستفادة (للأرشيف)
1. **اختبار models قبل التطوير**: فحص ListModels على المفتاح الفعلي قبل اختيار اسم نموذج
2. **Smoke test E2E بعد كل deploy**: `npm run test:eval` على الإنتاج كان سيكشف هذا فوراً
3. **Pin model versions**: ضمن documented constants — لا تعتمد على defaults قابلة للتغيير
