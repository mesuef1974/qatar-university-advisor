# شيك ليست-13 — تحسينات المنصة الشاملة (7 مبادرات)

**التاريخ**: 2026-04-26
**المرجع**: عصف ذهني CEO → موافقة شاملة "هيا كلها بلا استثناء"
**الهدف**: تجويد المنصة + بوت WhatsApp + قاعدة البيانات + الردود المؤتمتة
**الحالة العامة**: 🟡 IN PROGRESS

---

## 🏁 Sprint 1 — الجلسة الحالية (P0 — اليوم)

### #5 Persona + Tone Consistency
- [x] قراءة `lib/ai-system-prompt.js` الحالي
- [x] **اكتشاف**: Persona "مُرشِدي" موجودة وممتازة — أضفنا tweaks بدلاً من إعادة كتابة
- [x] إضافة Tone Calibration block (empathy + clarification + soft language)
- [x] إضافة Length Adapter block (5-7/2-4/جدول/خطوات حسب نوع السؤال)
- [ ] tests يتحققون من الـ persona consistency (ضمن #7)

### #7 Quality Eval Pipeline (Golden Set)
- [x] إنشاء `tests/golden/qa-set.json` — **50 سؤال** نموذجي (5 فئات × 10)
- [x] إجابات مرجعية: expectKeywords + mustNotContain لكل سؤال
- [x] script `tests/golden/run-eval.js` يحسب accuracy + per-category breakdown
- [x] threshold لـ baseline: 0.6 per item, 0.7 overall (CI gate via exit code)
- [x] CI integration عبر `npm run test:eval` (مضاف في package.json)

### #3 Conversation Memory + Personalization
- [x] تفعيل `getConversationHistory` في `/api/chat` (يمرّر history الفعلي بدلاً من `[]`)
- [x] قراءة `userProfile` من Supabase (profile_data JSONB) ودمجه
- [x] **opt-in pattern**: الـ behavior لا يتغير بدون `sessionId` (backward compat)
- [x] persistence fire-and-forget (لا يحجب المستخدم لو DB بطيء)
- [x] لا يكسر الـ web chat (sessionId اختياري)
- [x] لا regressions: 535/535 tests pass

### Sprint 1 — Quality Gates
- [ ] `npx tsc --noEmit` PASS
- [ ] `npx vitest run` PASS (no regressions)
- [ ] commit + push to `claude/quizzical-lewin-43d4d2`
- [ ] تحديث memory file

---

## 📅 Sprint 2 — الأسبوع 2 (P1)

### #1 Knowledge Base غني + Versioned
- [ ] migration: ALTER universities (deadline, apply_url, logo_url, qs_rank, ...)
- [ ] جدول `program_career_paths` (fit_score)
- [ ] جدول `knowledge_versions` (audit trail)
- [ ] ETL script يجلب البيانات الجديدة من sources موثوقة
- [ ] verification UI في `/admin`

### #2 RAG (Retrieval-Augmented Generation)
- [ ] إضافة embedding model (bge-m3 أو multilingual-e5)
- [ ] backfill embeddings للـ knowledge base كاملاً
- [ ] دالة `semanticSearch(query, k=5)` تعيد top chunks
- [ ] inject في Gemini prompt
- [ ] قياس quality قبل/بعد عبر eval pipeline (#7)

---

## 📅 Sprint 3 — الأسبوع 3 (P1)

### #4 WhatsApp Rich UX
- [ ] **Phase 1**: typing indicator + read receipts
- [ ] **Phase 2**: image messages (logos)
- [ ] **Phase 3**: PDF attachments (catalogues)
- [ ] **Phase 4**: scheduled reminders (WhatsApp templates approval needed)
- [ ] واجهة مماثلة في web chat

### #6 Analytics Dashboard
- [ ] صفحة `/admin/insights` بـ shadcn/ui
- [ ] KPIs: top queries, unanswered, drop-off, response latency
- [ ] weekly digest email (Vercel cron) للـ CEO
- [ ] PDPPL-safe (لا PII في الـ aggregates)

---

## 📊 KPIs (تُقاس بعد كل Sprint)

| KPI | Baseline | Target |
|-----|----------|--------|
| Eval accuracy (golden 50) | TBD | ≥ 90% |
| Mean response latency | ~1.4s | ≤ 1.0s |
| Unanswered questions rate | TBD | ≤ 5% |
| User profile completion | TBD | ≥ 70% بعد 5 رسائل |
| WhatsApp delivery rate | TBD | ≥ 99.5% |

---

## 🚦 سياسات Sprint 1

- **Pre-Approval Protocol**: CEO وافق صراحة على Sprint 1 كاملاً
- **Logic Gate قبل كل بند**
- **QA قبل كل push**
- **CEO لا يكتب كود**
- **Documentation-First**: تحديث هذا الملف بعد كل بند

---

**آخر تحديث**: 2026-04-26 — الجلسة الحالية
